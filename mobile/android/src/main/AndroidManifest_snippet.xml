<!-- Add these permissions above your <application> tag in AndroidManifest.xml -->
<uses-permission android:name="android.permission.INTERNET" />
<uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

<!-- 
For local testing against HTTP (non-HTTPS) localhost, ensure you add this inside your <application> tag:
android:usesCleartextTraffic="true"
-->

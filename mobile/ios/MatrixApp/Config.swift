import Foundation

struct Config {
    // Hardcoded API Base URL for local simulator testing
    // Change this to the production IP or domain when deploying
    static let baseURL = "http://localhost:8080/api"
}

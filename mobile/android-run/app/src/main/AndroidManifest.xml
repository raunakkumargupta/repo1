<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <!-- Required on Android 13+ (API 33+) for push notifications -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <!-- Keep polling service alive when device is idle -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />

    <application
        android:label="@string/app_name"
        android:icon="@drawable/matrix_logo"
        android:theme="@style/Theme.MatrixApp"
        android:usesCleartextTraffic="true">

        <activity android:name=".HackathonDetailActivity" android:exported="false" />
        <activity android:name=".FindTeamActivity" android:exported="false" />
        <activity android:name=".ApplicationsActivity" android:exported="false" />
        <activity android:name=".ProfileActivity" android:exported="false" />
        <activity android:name=".EditProfileActivity" android:exported="false" />
        <activity android:name=".MentorSupportActivity" android:exported="false" />
        <activity android:name=".ExploreActivity" android:exported="false" />
        <activity android:name=".RegisterActivity" android:exported="false" />
        <activity android:name=".DashboardActivity" android:exported="false" />

        <activity android:name=".LoginActivity" android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Local notification polling service (no Firebase needed) -->
        <service
            android:name=".notifications.NotificationPollingService"
            android:exported="false" />

    </application>
</manifest>
